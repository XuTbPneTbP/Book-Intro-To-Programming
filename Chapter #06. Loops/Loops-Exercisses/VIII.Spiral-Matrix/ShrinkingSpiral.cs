namespace VIII.Spiral_Matrix
{
    class ShrinkingSpiral
    {
        static void Main()
        {
            // 1. Fill the current cell. Represents single move in current direction
            // 2. Check wheather current direction should be changed and precalculate the moves in the new direction
            // 3. Move the current position to the next cell. Care about the position => up, down, right, left
            // 4. Data structure => Two-dimensional array

            int size = 4;

            int[,] shrinkingSpiral = new int[4, 4];

        }
    }
}
